# План розробки та випуску

Документація передує коду. Послідовність обов'язкова: завершити desktop single-player → VR single-player → онлайн. Базова геометрія 3D з M0.

План нижче зберігає початкові критерії приймання. Код усіх трьох режимів уже реалізовано за подальшим запитом користувача; це не означає проходження апаратних gate S2/S3. Поточні докази — у [звіті](10-implementation-status.md).

## Етап 1

| Крок | Робота | Залежності | Доказ завершення |
| --- | --- | --- | --- |
| M0 | Ініціалізувати TypeScript/Vite/Three.js, обрати версії, lockfile, lint/typecheck/test/build | Документація | Чистий install та build, мінімальна 3D-сцена в Chrome |
| M1 | Core: дані, config, PRNG, tick, 3D-рух і межі | M0 | T-001–005,019,021 |
| M2 | Їжа, поглинання, спавн, ріст, рейтинг, результати | M1 | T-006–013,016–018,020,022–023 |
| M3 | Боти й spatial index | M2 | T-014–015 та oracle/property tests |
| M4 | Desktop camera/input, Pointer Lock, меню, HUD, налаштування | M2,M3 | T-101–108 |
| M5 | Оптимізація, ресурси, UX-плейтест, регресія | M4 | T-109–110, звіт performance, gate S1 |

Gate S1: п'ятихвилинний 3D-матч працює у Chrome на ноутбуці; усі CORE/S1 пройдені; немає P0/P1. Лише після цього починається робота над VR.

## Етап 2

| Крок | Робота | Залежності | Доказ завершення |
| --- | --- | --- | --- |
| M6 | WebXR lifecycle, перевірка сумісності, XR camera rig | Gate S1 | T-201–202 |
| M7 | Контролери, 3D locomotion, snap turn, просторовий UI | M6 | T-203–207,210 |
| M8 | Quest-профіль, комфорт, фізичне приймання | M7 | T-208–209 і повна desktop-регресія, gate S2 |

Gate S2: одиночний матч на фізичному Quest 2 та збережена працездатність Chrome. Неперевірені апаратні вимоги блокують саме приймання VR.

## Етап 3

| Крок | Робота | Залежності | Доказ завершення |
| --- | --- | --- | --- |
| M9 | Схеми protocol, WSS, серверний core, кімнати/ready | Gate S2 | T-301–304,317 |
| M10 | Snapshots, interpolation, prediction/reconciliation | M9 | T-305–306,319 |
| M11 | Смерть/respawn, reconnect, ліміти, validation | M10 | T-307–314,318 |
| M12 | Нагрузочні сценарії, деплой, моніторинг, rollback | M11 | T-315–316,320, gate S3 |

Gate S3: Chrome і Quest 2 грають у спільному матчі, серверні рішення узгоджені, reconnect/захист/навантаження перевірені. Дати та строки не обіцяються до M0 і першого профілювання.

## Правила роботи

Кожна зміна прив'язана до вимоги/тесту. Послідовність: fixture або сценарій → реалізація → цільова перевірка → доречна регресія → документація.
Баланс зберігати у версійованому config. Зміни не підміняють його посеред матчу. Рендерер не може виправляти помилки симуляції локально.
Definition of Done: реалізовано acceptance criterion, змістовні тести PASS, typecheck/lint/build PASS, немає нових unhandled errors, актуальні документи, записані обмеження.
Назви гілок/PR та commit політика визначаються під час ініціалізації репозиторію; цей пакет не створює PR, сервер або сайт.

## Команди проєкту

Скрипти реалізовані у package.json:

| Команда | Призначення |
| --- | --- |
| npm ci | Відтворювана інсталяція з lockfile |
| npm run dev | Локальний клієнт та сервер |
| npm run lint / npm run typecheck | Статичні перевірки |
| npm test | Unit/integration |
| npm run test:e2e | Chrome E2E |
| npm run build | Production build |
| npm run dev:server | Сервер з автоматичним перезапуском |
| npm run test:load | Локальний smoke навантаження |

CI на кожному PR: install → lint/typecheck → unit/integration → build → Chrome smoke. Окремий артефакт тестів/trace на помилці. Server integration і production smoke вже входять у CI. Hardware VR та повне навантаження — перед випуском, не видавати емуляцію за апаратне приймання.
CI обмежує час job, використовує lockfile та не надає секрети стороннім PR.

## Середовища та розгортання

Local: Chrome localhost. Quest: HTTPS staging або документований локальний debug-тунель. Звичайний HTTP за LAN IP не вважати production-рішенням для WebXR.
Production: один Node.js-процес обслуговує dist та /socket. Render надає HTTPS/WSS; конфігурація безкоштовного розгортання — render.yaml.
Провайдера й бюджет обрати перед фактичним деплоєм; документація не санкціонує платні покупки.
Env сервера: PORT, ALLOWED_ORIGINS (або RENDER_EXTERNAL_URL), MAX_ROOMS, NODE_ENV. Необовʼязкова публічна змінна клієнта: VITE_SERVER_URL. За відсутності потрібної конфігурації сервер не починає слухати.
Static assets з content hash кешувати довго; HTML/manifest сумісності — коротко або no-cache. Спочатку сервер зі сумісним protocol/rules, потім client. Несумісна зміна вимагає drain старих матчів.

Health endpoints: /healthz — процес живий; /readyz — готовий приймати кімнати. Після SIGTERM вимкнути readiness та нові кімнати; дати поточним матчам до 330 s, потім повідомити SERVER_RESTART і закрити sockets. Аварійне падіння виявляється клієнтом через heartbeat/timeout.

## Спостереження і відновлення

Метрики: активні кімнати/люди, tick p95/p99 і lag, RSS/event-loop lag, disconnect rate, RTT, queue bytes, відхилені пакети, reconnect success. Клієнтські performance-дані для тестів локальні; постійну аналітику користувачів не вмикати автоматично.
Сигнали втручання: tick lag >100 ms протягом 30 s, RSS >80% ліміту, зростання disconnect >10% за 5 min за наявності >=20 сесій. Поріг SERVER_OVERLOAD діє окремо за 03.
При регресії: заборонити створення нових кімнат → зібрати build/config/метрики без секретів → drain → повернути попередні client/server build і config → smoke Chrome/Quest → відкрити прийом.
Зберігати попередні дві перевірені збірки й конфігурації. Бази даних у v1 немає; результати активних матчів при падінні не відновлюються.

## Після третього етапу

Лише окремою специфікацією: поділ, злиття, викидання маси, віруси, матчмейкінг, рейтинги, акаунти, голос, mobile, delta snapshots, interest management, кілька серверів/регіонів. Для кожної нової механіки потрібні desktop/VR/network тести.
